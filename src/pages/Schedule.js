import classes from './Schedule.module.css';

export default function Schedule({tasks}) {
    return (
        <>
         <h1 className={classes.header}>Your Task Is secefully Added!</h1>

        </>
    )
   
}